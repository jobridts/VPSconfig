---
- hosts: controller
  roles:
   - ansible_galaxy
  gather_facts: no

- hosts: vagrant
  roles:
    - common
    - apache
    - web
    - database
    - modoboa
    - kosssi.composer
    - phpmyadmin
  handlers:
    - include: handlers/main.yml
